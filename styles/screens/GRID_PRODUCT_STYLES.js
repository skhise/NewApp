// Importing
import {StyleSheet} from 'react-native';
import {scale} from 'react-native-size-matters';
import FONTS from '../../config/FONTS';
import COLORS from '../../config/COLORS';

// Exporting
export default GRID_PRODUCT_STYLES = StyleSheet.create({
  gridView: {
    flex: 1,
  },
});
